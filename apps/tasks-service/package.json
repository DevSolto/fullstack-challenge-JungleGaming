{
  "name": "@apps/tasks-service",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "node -e \"console.log('tasks-service: dev placeholder (S1)')\"",
    "build": "echo \"build placeholder\"",
    "lint": "eslint . --ext .ts,.tsx || true",
    "typecheck": "tsc -p tsconfig.json --noEmit || true",
    "clean": "rimraf dist || true"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "eslint": "^9.10.0",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0"
  },
  "dependencies": {
    "@repo/types": "0.0.0",
    "@repo/utils": "0.0.0",
    "@repo/tsconfig": "0.0.0"
  }
}